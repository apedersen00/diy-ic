## two_stage.py

import numpy as np
from mos_device import MosDevice

class TwoStage():
    def __init__(self) -> None:
        M1  = MosDevice()
        M2  = MosDevice()
        M3  = MosDevice()
        M4  = MosDevice()
        M5  = MosDevice()
        M6  = MosDevice()
        M7  = MosDevice()
        M8  = MosDevice()
        M9  = MosDevice()
        M10 = MosDevice()
        M11 = MosDevice()
        M12 = MosDevice()
        